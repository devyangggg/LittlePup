<Window x:Class="LittlePup.PetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LittlePup"
        Width="1" Height="1"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        ShowActivated="False"
        WindowState="Minimized">
    <!-- Intentionally empty. This window exists only to own the animated taskbar button + icon;
         it is kept minimized and never shown. -->
</Window>
