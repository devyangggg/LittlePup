// LittlePupTests.swift – placeholder test case; concrete tests are added per build step

import XCTest // XCTest is Apple's unit-testing framework

// Container for all LittlePup logic tests; populated starting at Step 2
final class LittlePupTests: XCTestCase {
    // Intentionally empty for Step 1; each subsequent step adds its own test methods here
}
